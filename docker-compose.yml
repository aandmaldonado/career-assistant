version: '3.8'

services:
  app:
    build: .
    container_name: career-assistant-app
    ports:
      - "8501:8501"
    volumes:
      - ./:/app
    env_file:
      - .env
    environment:
      - PYTHONPATH=/app
    restart: unless-stopped
